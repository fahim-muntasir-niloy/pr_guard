{
  "name": "pr-guard-extension",
  "displayName": "PR Guard",
  "description": "VS Code extension for PR Guard CLI",
  "version": "0.0.1",
  "publisher": "pr-guard",
  "repository": {
    "type": "git",
    "url": "https://github.com/fahim-muntasir-niloy/pr_guard.git"
  },
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "main": "./out/extension.js",
  "activationEvents": [
    "onView:pr-guard.sidebarView",
    "onCommand:pr-guard.review",
    "onCommand:pr-guard.chat",
    "onCommand:pr-guard.status",
    "onCommand:pr-guard.oneClickPR"
  ],
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "pr-guard-sidebar",
          "title": "PR Guard",
          "icon": "resources/icon.svg"
        }
      ]
    },
    "views": {
      "pr-guard-sidebar": [
        {
          "type": "webview",
          "id": "pr-guard.sidebarView",
          "name": "PR Guard"
        }
      ]
    },
    "commands": [
      {
        "command": "pr-guard.review",
        "title": "PR Guard: Review"
      },
      {
        "command": "pr-guard.chat",
        "title": "PR Guard: Chat"
      },
      {
        "command": "pr-guard.status",
        "title": "PR Guard: Status"
      },
      {
        "command": "pr-guard.oneClickPR",
        "title": "PR Guard: Create One Click PR"
      }
    ],
    "configuration": {
      "type": "object",
      "title": "PR Guard",
      "properties": {
        "prGuard.executablePath": {
          "type": "string",
          "default": "",
          "description": "Path to the pr-guard executable (optional override)."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./"
  },
  "devDependencies": {
    "@types/markdown-it": "^14.0.0",
    "@types/node": "^18.x",
    "@types/vscode": "^1.85.0",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "@vscode/codicons": "^0.0.44",
    "markdown-it": "^14.0.0"
  }
}
